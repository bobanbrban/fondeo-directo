export function removemail(state=[],action) {
	switch(action.types) {
		case 'REMOVE_MAIL':
            return (this.state)
           
  //          ...state.slice(0,i),
  //          {...state.slice[i]('') },
  //          ...state.slice(i+1),  

  //       
 default:
	     return state;
   }
}