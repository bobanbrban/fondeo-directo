export function markspam(state=[],action) {
	switch(action.types) {
		case 'MARK_SPAM':
   return (this.state)
           
  //          ...state.slice(0,i),
  //          {...state.slice[i]('') },
  //          ...state.slice(i+1),  

  //       
 default:
       return state;
   }
}