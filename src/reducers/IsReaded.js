export function isreaded(state=[],action) {
	switch(action.types) {
		case 'CHANGE_READED':
        
 return (this.state)
           
  //          ...state.slice(0,i),
  //          {...state.slice[i]('') },
  //          ...state.slice(i+1),  

  //       
 default:
       return state;
   }
}

